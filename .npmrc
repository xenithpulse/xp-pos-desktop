# next-auth v4's peer range for `next` caps at 15, but this project runs Next 16.
# No next-auth release targets Next 16 yet, so relax strict peer resolution to
# let `npm install` complete. Remove this once next-auth publishes a Next-16 peer.
legacy-peer-deps=true
